# ── DocuChat Environment Configuration ──────────────────────────────────────
# Copy this file to .env and fill in your values

# ── LLM Provider ──────────────────────────────────────────────────────────
# Choose one: anthropic | openai | ollama
LLM_PROVIDER=anthropic

# API Keys (set the one matching your provider)
ANTHROPIC_API_KEY=sk-ant-your-key-here
# OPENAI_API_KEY=sk-your-openai-key-here

# Model name (optional — defaults are set per provider)
# LLM_MODEL=claude-haiku-4-5-20251001      # Anthropic (fast, cheap)
# LLM_MODEL=claude-sonnet-4-6              # Anthropic (better quality)
# LLM_MODEL=gpt-4o-mini                   # OpenAI
# LLM_MODEL=llama3.2                       # Ollama (local)

# Ollama URL (only if using Ollama)
# OLLAMA_URL=http://localhost:11434

# ── Server ────────────────────────────────────────────────────────────────
PORT=5000
FLASK_ENV=development
SECRET_KEY=change-this-to-a-random-secret-in-production

# ── Optional: Semantic Search ─────────────────────────────────────────────
# Install sentence-transformers for better search: pip install sentence-transformers
# ENABLE_SEMANTIC_SEARCH=true
