flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# Document processing
pdfplumber>=0.10.0
python-docx>=1.1.0

# LLM providers (install the one you need)
anthropic>=0.36.0         # For Claude
# openai>=1.50.0          # For GPT-4
# requests>=2.31.0        # For Ollama (local)

# Optional: semantic search (recommended for better results)
# sentence-transformers>=2.7.0
# numpy>=1.26.0

# Optional: async task queue (for large files)
# celery>=5.3.0
# redis>=5.0.0

gunicorn>=22.0.0           # Production server